import React, { Component } from 'react';
import '../materialize/css/materialize.css';

class Carousel extends Component {
  render() {
    return(
      <div className="carousel responsive-img">
        <a className="carousel-item" href="#one!"></a>
      </div>

      );
  }
}

export default Carousel;
