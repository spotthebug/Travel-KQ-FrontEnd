import React, { Component } from "react";

class Header extends Component {
  render() {
    return (
      <nav>
        <a href="#">signup</a> <br />
        <a href="#">login</a>
      </nav>
    );
  }
}

export default Header;
